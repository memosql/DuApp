import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.germanlearning.app',
  appName: 'German Learning App',
  webDir: 'dist'
};

export default config;
